/**
 * The types for a Login Submission by a user.
 * @param password The user's password.
 * @param username The user's username.
 */
export interface LoginSubmission {
    password: string
    username: string
  }